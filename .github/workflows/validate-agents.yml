name: Validate agents

on:
  push:
    branches: [main]
  pull_request:

jobs:
  frontmatter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Valida el frontmatter dels agents
        run: bash scripts/validate-agent-frontmatter.sh
